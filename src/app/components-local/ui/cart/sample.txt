import { TrashIcon } from "lucide-react";
import Image from "next/image";
import Button from "../button/button";

const CartItem = () => {
    return (
        <article className="relative flex flex-col sm:flex-row gap-4 w-full max-w-3xl p-4 bg-blue-100 border-b border-gray-300 rounded-md shadow-sm">
            <div className="flex-shrink-0 flex gap-3 items-start w-full sm:w-2/3">
                <input type="checkbox" name="pick" id="pick" aria-label="checkout item" className="mt-1" />
                <Image
                    src="/si.svg"
                    width={80}
                    height={80}
                    alt="foto produk"
                    className="object-cover rounded-md"
                />
                <div className="flex flex-col justify-between">
                    <p className="text-lg font-semibold leading-tight">Hair Cut for Short Hair</p>
                    <p className="text-lg font-semibold leading-tight text-gray-700">Rp150.000</p>
                    <p className="text-sm text-gray-600 mt-1">
                        Jadwal Pesanan: <span>27 Oktober 2023 14:15</span>
                    </p>
                </div>
            </div>

            <div className="flex flex-col justify-end w-full sm:w-1/3">
                <div className="flex items-center gap-2 border">
                    <input
                        type="text"
                        aria-label="catatan pesanan"
                        placeholder="Catatan untuk pegawai"
                        name="note"
                        id="note"
                        className="flex-grow outline-none text-sm bg-white p-2 rounded-md"
                    />
                    <Button
                        aria-label="hapus item"
                        className="text-yellow-600 hover:text-yellow-800 transition"
                    >
                        <TrashIcon size={24} />
                    </Button>
                </div>
            </div>
        </article>
    );
};

export default CartItem;
